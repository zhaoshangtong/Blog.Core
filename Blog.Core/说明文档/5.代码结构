1. Models是整个项目的数据库表实体类，手动创建。
   ViewModels是DTO实体类，通过AutoMap自动转换.
   repository就是一个管理数据持久层的，它负责数据的CRUD.
   （也称仓储）协调领域和数据映射层，利用类似与集合的接口来访问领域对象。
   它的存在让领域层感觉不到数据访问层的存在，它提供一个类似集合的接口提供给领域层进行领域对象的访问。
   IRepository层，提供了所有的操作接口。
   Service层只负责将Repository仓储层的数据进行调用，至于如何是与数据库交互的，它不去管
